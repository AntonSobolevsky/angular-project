language: node_js
node_js:
- '10.14'
addons:
  apt:
    sources:
    - google-chrome
    packages:
    - google-chrome-stable
cache:
  directories:
  - node_modules
script:
- ng test --watch=false --progress=false --browsers=ChromeHeadlessCI
